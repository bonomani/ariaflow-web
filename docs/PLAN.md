# Plan

*No open items.*

Recent work:
- Phase 1 complete — `TestBackendFieldCoverage` now auto-discovers fields from
  backend `openapi.yaml`. No more hand-maintained `EXPECTED_FIELDS` dict.
- FE-13 filed for 18 diagnostic/meta fields not displayed (all documented in
  `KNOWN_UNUSED` with reasons).
- BG-3 marked consumed in paired gaps file.
